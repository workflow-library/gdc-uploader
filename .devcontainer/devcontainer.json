{
  "postCreateCommand": "docker login ghcr.io -u YOUR_USERNAME -p $GHCR_PAT",
  "secrets": {
    "GHCR_PAT": {
      "description": "PAT with write:packages for GHCR"
    }
  },
  "customizations": {
    "codespaces": { "permissions": { "contents": "write", "packages": "write" } }
  }
}
