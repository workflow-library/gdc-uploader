{
  "customizations": {
    "codespaces": {
      "repositories": {
        "open-workflow-library/owlkit": {
          "permissions": "write-all"
        },
        "open-workflow-library/owlstacks": {
          "permissions": "write-all"
        }
      }
    }
  },
  "postCreateCommand": "mkdir -p zed && git clone https://github.com/open-workflow-library/owlkit.git zed/owlkit && git clone https://github.com/open-workflow-library/owlstacks.git zed/owlstacks && npm install -g @anthropic-ai/claude-code"
}
